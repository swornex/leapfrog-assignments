import { items } from "../../images.js";
import _BaseDiamond from "./_base.js";

export default class RedDiamond extends _BaseDiamond {
  constructor(x, y, width, height) {
    super(x, y, width, height, 300, 64, 0);
  }
}
